{% extends 'base.html.twig' %}

{% block title %}Hello ArticlesController!{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>Hello! ✅</h1>

    <ul>
        {% for item in articles %}
            <li><a href="{{ path('article', {name: item}) }}">{{ item }}</a></li>
        {% endfor %}
    </ul>
    <p><a href="{{ path('about') }}">O nas</a></p>
</div>
{% endblock %}
